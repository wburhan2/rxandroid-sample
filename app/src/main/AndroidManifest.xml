<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.rxandroid">

	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".activities.MainActivity"
			android:screenOrientation="portrait">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".activities.ZipActivity"
			android:label="Zip"
			android:screenOrientation="portrait"/>
		<activity android:name=".activities.DebounceActivity"
			android:label="Debounce"
			android:screenOrientation="portrait"/>
		<activity android:name=".activities.FlatMapActivity"
			android:label="FlatMap"
			android:screenOrientation="portrait"/>
		<activity android:name=".activities.CombineLatestActivity"
			android:label="CombineLatest"
			android:screenOrientation="portrait"/>
	</application>

</manifest>
